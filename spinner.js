import React from 'react';
import './spinner.css'; // Ensure this CSS file is updated

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
